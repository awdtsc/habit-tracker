<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Habit Tracker 共通設定
    |--------------------------------------------------------------------------
    |
    | アプリ全体で使う「習慣まわり」の設定です。
    | - timezone: 習慣やログの基準となるタイムゾーン
    | - auto_create_log_on_scheduled:
    |     true  → スケジュール時刻になったら自動で habit_logs / RemindTask を作る設計を前提にする
    |     false → 自動生成せず、ユーザー操作時にだけログを作る
    |
    */

    'timezone' => 'Asia/Tokyo',

    // 将来の DailyInit / 自動リマインド設計で使うフラグ（今は true のままでOK）
    'auto_create_log_on_scheduled' => true,

    /*
    |--------------------------------------------------------------------------
    | 時間帯スロット定義（morning / noon / evening / night）
    |--------------------------------------------------------------------------
    |
    | HabitScheduleService::resolveSlotFromTime() から参照されます。
    |
    | - code:  time_slot のコード（1〜4）
    | - start: 開始「時」（0〜23）
    | - end:   終了「時」（0〜23）
    |
    | ※ start <= end  の場合  → 同日内の範囲（例: 5〜10時）
    |    start > end   の場合  → 日付またぎ（例: 21〜4時 → 21:00〜23:59, 00:00〜04:59）
    |
    | あとで「朝は 4時〜9時にしたい」みたいな調整をしたくなったら、
    | ここを書き換えるだけでサーバー側の判定が一括で変わります。
    |
    */

    'slots' => [

        // 朝: 05:00〜10:59
        'morning' => [
            'code'  => 1,
            'start' => 5,
            'end'   => 10,
        ],

        // 昼: 11:00〜16:59
        'noon' => [
            'code'  => 2,
            'start' => 11,
            'end'   => 16,
        ],

        // 夕方: 17:00〜20:59
        'evening' => [
            'code'  => 3,
            'start' => 17,
            'end'   => 20,
        ],

        // 夜: 21:00〜23:59, 00:00〜04:59（start > end なので日またぎ）
        'night' => [
            'code'  => 4,
            'start' => 21,
            'end'   => 4,
        ],
    ],
];